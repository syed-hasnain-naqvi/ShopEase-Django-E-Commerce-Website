// simple placeholder - later we can add sliders, quick view etc.
document.addEventListener('DOMContentLoaded', function () {
  // console.log('Home scripts loaded');
});
// Newsletter subscription popup

/*
document.addEventListener("DOMContentLoaded", () => {
  const form = document.querySelector(".newsletter-form");
  if (form) {
    form.addEventListener("submit", (e) => {
      e.preventDefault();

      // Create popup
      const popup = document.createElement("div");
      popup.className = "subscription-popup";
      popup.innerHTML = `
        <div class="popup-content">
          <h4>✅ Thank You for Subscribing!</h4>
          <p>You’ll start receiving updates soon.</p>
          <button id="closePopup" class="btn btn-dark mt-2">Close</button>
        </div>
      `;

      document.body.appendChild(popup);

      // Close popup
      document.getElementById("closePopup").addEventListener("click", () => {
        popup.remove();
      });
    });
  }
});
*/